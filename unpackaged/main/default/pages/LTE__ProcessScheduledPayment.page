<apex:page standardStylesheets="false" showHeader="false" sidebar="false" cache="true" standardController="OrderApi__Scheduled_Payment__c">
    <apex:includeLightning />
    <div id="lightning" />
    <script>
      $Lightning.use("LTE:SalesOrderApp", function() {
          $Lightning.createComponent("LTE:processScheduledPayment", {
						recordId : "{!$CurrentPage.parameters.id}",
						theme : "{!$User.UITheme}"
          }, "lightning", function(cmp, status, error) {
              console.log(error);
          });
      });
	</script>

</apex:page>